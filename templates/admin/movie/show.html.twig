{% extends "layouts/admin.html.twig" %}

{% block body %}
<div class="row">
    <div class="col">
        <h1>{{page_title}}</h1>
        {% include "parts/_alerts.html.twig" %}

        <div class="overflow-auto">
            <table class="table table-bordered table-hover table-sm">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ movie.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ movie.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <a href="{{ path('app_admin_movie_index') }}" class="btn btn-sm btn-secondary my-2">back to list</a>

        <a href="{{ path('app_admin_movie_edit', {'id': movie.id}) }}" class="btn btn-sm btn-primary">edit</a>

        {{ include('admin/movie/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
